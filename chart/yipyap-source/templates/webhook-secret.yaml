apiVersion: v1
kind: Secret
metadata:
  name: yipyap-source-webhook-certs
  namespace: {{ include "yipyap-source.namespace" . }}
  labels:
    {{- include "yipyap-source.labels" . | nindent 4 }}
# Populated at webhook startup by knative.dev/pkg/webhook/certificates —
# self-signed; no cert-manager dependency and no chart-side TLS material.
